# Force Unix line endings for scripts — prevents "bad interpreter" errors
# when the repo is edited on Windows before copying to a Pi.
*.sh   text eol=lf
*.py   text eol=lf
*.md   text eol=lf
*.txt  text eol=lf
